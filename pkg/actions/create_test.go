package actions_test

import (
	"testing"
)

func TestCreateNote(t *testing.T) {
	// check if vault.default is called
	// check what happens if it throws error
	// check if uri.Construct is called with correct parameters
	// check if it returns the correct uri
}
